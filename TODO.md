# FineShell 开发 Todo List

> 基线日期：2026-07-22
>
> 对照文档：`FEATURES.md`
>
> 当前阶段：已完成真实 SSH 终端 P0、基础 SFTP P1、主机管理与可靠性 P2-A 至 P2-C、服务器资源/网络/进程监控 P3-A 至 P3-C、代理连接 P4-A、跳板机连接 P4-B、本地/远程/动态端口转发 P4-C、SSH Agent 与集中密钥管理 P4-D、传输队列与批量拖拽 P5-A 至 P5-B、远程文本安全编辑 P5-C、外部编辑器自动同步 P5-D、快速目录与路径历史 P5-E、远程剪贴板与拖拽移动 P5-F、终端效率工具 P6-A 至 P6-C、安全管理 P7-A 至 P7-C，以及工程质量 P8-A。

## 状态说明

- `[x]` 已实现：已有可操作代码，并完成基础编译检查。
- `[~]` 部分实现：界面或本地流程已完成，但缺少真实后端能力或完整交互。
- `[ ]` 未实现：当前代码中没有对应功能。

## 一、当前已经实现

### 1. 应用基础

- [x] 使用 Tauri 2、React、TypeScript 和 Vite 初始化桌面项目。
- [x] 接入 Arco Design React 组件库。
- [x] 使用 Arco `ResizeBox.SplitGroup` 构建三块可调整布局：左侧服务器监控、右上终端、右下 SFTP。
- [x] 主页面没有应用内 Header。
- [x] 应用内组件不使用阴影，保留操作系统原生窗口阴影。
- [x] 文件列表、监控面板、抽屉和管理页面使用统一的细滚动条，终端使用深色适配样式。
- [x] macOS 原生菜单和系统文件选择框完成简体中文本地化。
- [x] 设置使用独立原生窗口，可从系统菜单或 `Command/Ctrl + ,` 打开并复用已有窗口。
- [x] 设置窗口使用左侧分类菜单与右侧设置内容的双栏布局。
- [x] 已处理 SFTP 路径输入框聚焦时的粗边框问题。
- [x] 前端生产构建和 Tauri Rust 编译检查可以通过。

### 2. 主机管理

- [x] 主机管理作为终端标签栏中不可关闭的固定首页，不再使用独立窗口。
- [x] 首页直接展示主机分组与列表，不再嵌套主机/历史标签页。
- [x] 连接历史作为首页工具栏按钮，使用 Arco 对话框展示快速连接和历史记录。
- [x] 配置维护入口集中到独立设置窗口。
- [x] 支持新增、编辑和删除主机。
- [x] 支持按名称、地址、用户名或分组搜索主机。
- [x] 支持主机名称、地址、端口、用户名和分组字段。
- [x] 新增主机直接使用设置中的连接默认值，表单不重复展示可靠性参数。
- [x] 主机指纹由首次连接确认流程自动维护，连接目标变化时自动清除旧指纹。
- [x] 主机信息和连接历史使用版本化 IndexedDB 配置库持久化。
- [x] 支持快速连接表单。
- [x] 支持最近连接历史和历史重连入口。
- [x] 连接历史按目标去重，支持 20、50、100 条或不限数量，以及 7、30、90 天或永久保留策略。

### 3. 主窗口会话界面

- [x] 首页选择连接目标后直接创建并激活终端会话。
- [x] 连接目标会在主窗口创建终端会话标签。
- [x] 每次点击连接均创建独立会话标签，同一主机可以并行打开多个会话。
- [x] 同一主机的并行会话标签按打开顺序显示编号，关闭后自动重新编号。
- [x] 终端标签支持切换、关闭和新增入口，并包含不可关闭的固定首页。
- [x] 终端标签栏使用紧凑的 40px 高度。
- [x] 终端区域始终显示多标签栏，没有“终端”标题和“打开连接”按钮。
- [x] 左侧服务器监控面板显示远程系统信息和资源图表。
- [x] 支持从终端标签关闭当前会话。

### 4. SSH 终端最小闭环

- [x] 支持密码认证，密码保存在操作系统凭据库，不进入 `localStorage`。
- [x] 支持真实 SSH 握手、认证、远程 Shell 和 PTY。
- [x] 使用 xterm.js 渲染 ANSI 颜色、光标、选择区和交互式终端程序。
- [x] 支持键盘输入、流式输出和终端行列尺寸同步。
- [x] 支持终端内容搜索、高亮、匹配计数和双向跳转。
- [x] 支持集中管理快捷命令、动态参数填写以及填入或直接执行。
- [x] 支持主机 SHA256 指纹预校验，并在首次成功连接后记录指纹。
- [x] 支持连接中、已连接、失败、已断开和重连中状态。
- [x] 支持连接错误展示和失败后的手动重连。
- [x] 关闭标签可以取消正在握手的连接或释放已连接会话。

## 二、部分实现

### 1. 远程连接管理

- [x] 已支持真实密码、私钥、SSH Agent、SOCKS5、HTTP CONNECT 代理、一级跳板机及本地/远程/动态端口转发。
- [x] 已有多标签、连接超时、失败原因、SSH 保活和有限次数自动重连策略。
- [~] 已支持多层分组树、筛选、持久化排序和主机复制，尚未实现剪切和粘贴。
- [x] 保存主机和快速连接均支持密码、集中私钥或 SSH Agent 认证。

### 2. 终端

- [~] 已有真实多标签终端、远程 PTY、输入输出流和尺寸同步。
- [~] 已支持终端字体、字号、光标、回滚行数、内容搜索和快捷命令；尚未实现命令历史增强和配色主题。

### 3. SFTP

- [x] 已完成终端与 SFTP 同屏布局，并接入真实 SFTP 服务。
- [x] 支持远程目录读取、路径输入跳转、返回上级、刷新和双击目录导航。
- [x] 文件列表使用 `react-sticky` 实现滚动时表头吸顶。
- [x] 支持单文件与批量上传下载、拖拽上传、实时进度和失败重试。
- [x] 传输任务支持排队、并发限制、暂停、继续和取消，目录操作不会被大文件传输阻塞。
- [x] 支持新建目录、重命名和删除文件或空目录。
- [x] 支持全局控制隐藏文件显示和文件删除确认。
- [x] 支持覆盖确认、权限错误展示、会话失效提示和重新连接。

### 4. 服务器监控

- [x] 左侧面板展示远程系统信息和运行时间，监控刷新间隔可全局配置。
- [x] 左侧面板使用自适应宽度 VChart 展示 CPU、内存和磁盘当前占用及 CPU、内存趋势，图表 Tooltip 和数值轴使用百分比格式。

### 5. 本地配置

- [x] 主机和连接历史保存在版本化 IndexedDB 配置库，并自动迁移旧 `localStorage` 数据。
- [x] 密码和私钥口令保存在操作系统凭据库；主机、集中密钥元数据和普通配置已完成迁移、导入导出、自动备份、恢复和 30 天删除回收站。

### 6. 全局设置

- [x] 终端：字体、字号、光标样式、光标闪烁和回滚行数。
- [x] 文件管理：隐藏文件显示、删除前确认和默认外部编辑器。
- [x] 服务器监控：资源采样刷新间隔。
- [x] 连接：新建主机与快速连接的超时、保活和自动重连默认值。
- [x] 代理：集中管理 SOCKS5 和 HTTP CONNECT 代理，主机与快速连接可选择代理。
- [x] 密钥：集中管理可复用私钥及其系统凭据库口令，主机与快速连接只保存密钥引用。
- [x] 快捷命令：集中管理命令模板、分组、备注和动态参数。
- [x] 配置维护：配置导入导出、自动备份恢复和主机回收站。
- [x] 安全：集中管理已知主机指纹，并提供连接历史保留策略与遗留系统凭据扫描清理。
- [x] 设置写入版本化配置数据库并参与配置导入导出；全局默认值不会覆盖已有主机的专属连接参数。

## 三、尚未实现

### 1. SSH 与连接能力

- [x] 私钥文件认证和私钥口令安全存储。
- [x] SSH Agent 和集中密钥管理。
- [x] 首次连接指纹确认对话框和指纹变更警告。
- [x] SOCKS5 和 HTTP CONNECT 代理，支持无认证或用户名密码认证。
- [x] 一级跳板机连接，跳板机自身可通过 SOCKS5 或 HTTP CONNECT 代理接入。
- [x] 已完成本地、远程和动态 SOCKS5 端口转发。
- [ ] Windows 远程桌面连接。
- [x] SSH 保活、连接超时、断线检测和有限次数自动重连。

### 2. 完整终端能力

- [ ] 命令历史、自动补全和智能匹配。
- [x] 快捷命令面板和带动态参数的自定义命令。
- [x] 终端内容搜索与高亮。
- [~] 已支持字体、字号和光标设置；尚未支持配色方案、行高、提示音和背景设置。
- [ ] Zmodem `rz`、`sz` 文件传输。

### 3. 完整 SFTP 能力

- [x] 远程目录读取和路径导航。
- [x] 已支持上传、下载、批量任务、进度显示、失败重试、暂停、继续和取消。
- [x] 支持拖拽上传，以及将单个或多个远程项目拖入目录完成移动。
- [x] 支持新建、重命名、删除、复制、剪切和粘贴，并提供同名冲突策略。
- [x] 文件权限查看与修改。
- [x] 远程 UTF-8 文本文件打开、编辑、冲突检测和安全回传。
- [x] 远程 UTF-8 文本文件可使用系统默认应用、已配置编辑器或临时选择的本地工具打开，保存后自动安全同步。
- [ ] 文件夹打包传输、自动压缩和解压。
- [ ] 跟随终端当前目录。
- [x] 快速目录收藏、最近访问记录和路径输入匹配。

### 4. 服务器监控

- [x] 已实现 CPU、内存、磁盘和网络流量采集与图表展示。
- [x] 已实现可配置目标的 Ping 延迟、丢包和 Traceroute 诊断。
- [x] 已实现进程列表、搜索、资源排序以及 TERM/KILL 进程管理。
- [x] 已实现端口监听和网络连接状态列表，支持状态筛选和手动刷新。
- [~] 已实现操作系统、内核、主机名和运行时间采集展示；尚未采集详细硬件型号。
- [ ] 不同 Linux/Unix 系统命令输出的兼容解析。

### 5. 网络加速

- [ ] SSH 和远程桌面连接加速。
- [ ] 加速节点选择和自动切换。
- [ ] 提供给第三方 SSH 客户端使用的本地转发入口。

### 6. 云端同步与配置保护

- [ ] 用户账号和同步服务。
- [ ] 主机、代理、密钥、快捷命令和历史记录同步。
- [ ] 本地加密、同步密码、完整性校验和冲突解决。
- [x] 本地配置自动备份和恢复。
- [x] 删除连接回收站及 30 天恢复机制。

### 7. 工程质量

- [x] 前后端共享的连接命令、事件和错误类型定义。
- [x] 敏感数据安全存储，密码不进入 `localStorage`。
- [x] 日志分级、敏感信息脱敏和可导出的诊断日志。
- [ ] 单元测试、组件测试、Rust 集成测试和桌面端端到端测试。
- [ ] Windows、macOS 和 Linux 的兼容性测试。
- [ ] 安装包、应用图标、签名、自动更新和发布流程。
- [~] VChart 监控面板已按需拆分，主入口仍有大体积资源构建警告。

## 四、建议实施顺序

### P0：完成一条真实 SSH 连接闭环

P0 实现和真实 SSH 服务器在线验收均已完成。

- [x] 扩展主机模型，加入认证方式、连接超时和主机指纹信息。
- [x] 在 Rust 端建立 SSH 连接服务和会话生命周期管理。
- [x] 定义 `connecting`、`connected`、`failed`、`disconnected`、`reconnecting` 状态。
- [x] 建立前端与 Rust 之间的输入、输出、尺寸变化和关闭事件协议。
- [x] 接入成熟的终端渲染组件，承载 ANSI、光标和键盘交互。
- [x] 将当前静态终端面板替换为真实终端实例。
- [x] 在终端标签和服务器监控面板中展示连接状态及错误。
- [x] 实现主动断开、异常断开和资源释放。

#### P0 验收标准

- [x] 用户可以保存一个密码认证的 SSH 主机。
- [x] 点击连接后能看到明确的连接中、成功或失败状态。
- [x] 使用真实 SSH 服务器验证命令执行和持续输出。
- [x] 使用真实 SSH 服务器验证窗口变化后的远程 PTY 尺寸。
- [x] 关闭标签后会取消握手或关闭会话，并释放事件监听。
- [x] 密码不以明文写入 `localStorage` 或日志。

#### P0 在线验收记录

- [x] 密码认证主机可以完成保存、连接和状态更新。
- [x] 内置终端可以接收远程登录输出、发送命令并持续显示返回内容。
- [x] 应用窗口变化后，远程 PTY 从 `25×120` 同步更新为 `52×264`。
- [x] 主动关闭会话后，服务器端对应 TTY 被释放。

### P1：实现基础 SFTP

- [x] 复用主机认证配置建立 SFTP 会话。
- [x] 实现目录列表、路径导航和刷新。
- [x] 实现单文件上传、下载及进度显示。
- [x] 实现新建目录、重命名和删除。
- [x] 处理覆盖确认、权限不足、断线和重试。

#### P1 在线验收记录

- [x] Rust 在线测试通过真实服务器完成建连、目录读取、新建、写入、重命名、读取和清理。
- [x] FineShell 原生界面成功加载远程主目录并展示类型、大小、权限和修改时间。
- [x] 原生界面完成路径跳转、新建目录、上传、进度展示、下载和重命名。
- [x] 下载内容与上传内容逐字节一致，测试文件和远程目录已清理。

### P2：补齐主机管理与可靠性

- [x] 增加密码和私钥认证配置界面。
- [x] 增加 SOCKS5 和 HTTP CONNECT 代理配置界面。
- [x] 增加 SSH Agent 配置界面。
- [x] 增加跳板机配置界面和真实连接链路。
- [x] 增加分组树、排序、复制和导入导出。
- [x] 将普通配置迁移到版本化本地数据库。
- [x] 将现有密码和私钥口令迁移到操作系统安全存储。
- [x] 增加配置备份、恢复和删除回收站。
- [x] 增加连接保活、有限次数自动重连和主机指纹安全流程。

#### P2-A 验收记录

- [x] 保存主机和快速连接支持密码或私钥认证，私钥口令不进入普通配置。
- [x] 真实测试服务器通过私钥 SSH 认证和完整 SFTP 往返测试，临时公钥已清理。
- [x] 未知主机会在认证前暂停并返回 SHA256 指纹，确认后才读取认证凭据。
- [x] 已保存指纹变化时展示新旧值，不会静默发送认证凭据。
- [x] 终端和 SFTP 会话启用可配置保活，网络错误按 1、2、4 秒退避自动重连。
- [x] 真实测试服务器通过空闲会话 Keepalive 验收，临时测试公钥已清理。
- [x] 正常退出远程 Shell 不自动重连，关闭标签会取消连接和重连定时器。

#### P2-B 验收记录

- [x] 主机和最近连接记录写入带版本号的 IndexedDB 配置库。
- [x] 首次启动会迁移旧 `localStorage` 数据，数据库写入成功后再清理旧缓存。
- [x] 配置读取会过滤损坏记录和未知敏感字段，且保留最近 50 条连接记录上限。
- [x] 配置可以导入和导出带格式标识、版本号的 JSON，文件不包含密码或私钥口令。
- [x] 导入和恢复前自动创建当前配置快照，最多保留最近 10 份，并可从设置窗口恢复。
- [x] 删除主机会先进入回收站并保留认证凭据，可在 30 天内恢复。
- [x] 永久删除或 30 天到期后清理回收站记录及对应系统凭据。
- [x] 活动主机与回收站记录 ID 冲突时不会误删活动主机凭据。

#### P2-C 验收记录

- [x] 主机首页使用树型表格展示多层分组，并显示各分组节点的主机数量。
- [x] 使用 `生产/华东` 形式保存层级分组，分组主机位于对应节点下，未分组主机位于根节点。
- [x] 名称、地址、用户名或分组搜索会保留匹配主机的完整父级路径。
- [x] 支持按添加顺序、名称、地址和最近连接排序，排序偏好持久化到配置数据库。
- [x] 排序偏好随配置导入导出和备份恢复，旧版配置升级后保持添加顺序。
- [x] 支持复制主机并自动生成唯一名称和新 ID，复制结果重置最近连接时间。
- [x] 密码和私钥口令在 Rust 内部通过系统凭据库复制，不会暴露给前端或普通配置。
- [x] 主机副本保存失败时会清理新 ID 对应的凭据，避免遗留孤立敏感数据。

### P3：监控和高级功能

- [x] 先实现系统信息、CPU、内存和磁盘的基础采集与左侧图表展示。
- [x] 已实现网络流量趋势、Ping 延迟/丢包、端口连接状态和路由追踪。
- [x] 已实现进程列表、搜索、排序和进程管理。
- [ ] 在核心 SSH/SFTP 稳定后，再评估远程桌面、网络加速和云同步。

#### P3-A 验收记录

- [x] 复用已认证 SSH 会话采集远程数据，不创建第二次登录或向前端传递凭据。
- [x] 展示操作系统、内核、主机名、运行时间和 1/5/15 分钟负载。
- [x] 每 5 秒采集 CPU、内存和根磁盘占用，并在左侧栏显示当前占用图。
- [x] 保留最近 24 个采样点，以面积图展示约 2 分钟 CPU 和内存趋势。
- [x] 会话断开时停止轮询，采集错误就地展示且后续轮询可以自动恢复。
- [x] VChart 使用独立懒加载资源块，不增加主窗口首屏主入口体积。

#### P3-B 验收记录

- [x] 在基础采样中读取非回环网卡累计收发字节，并按采样间隔计算实时上传、下载速率。
- [x] 左侧栏显示当前网络速率、累计流量和最近约 2 分钟的 VChart 流量趋势。
- [x] 网络诊断使用独立抽屉，不占用服务器监控和文件管理面板的常驻空间。
- [x] Ping 支持自定义域名或 IP，展示可达状态、丢包率和最低、平均、最高延迟。
- [x] 端口与连接列表展示协议、状态、本地地址、远端地址和可用的进程信息，并支持状态筛选。
- [x] 路由追踪限制为 12 跳并以时间轴展示，兼容 `traceroute`、BusyBox 和 `tracepath` 输出。
- [x] 诊断目标经过 Rust 端白名单校验，命令缺失或采集失败只返回诊断错误，不关闭终端会话。

#### P3-C 验收记录

- [x] 复用已认证 SSH 会话采集最多 500 条进程，不创建额外登录或向前端传递凭据。
- [x] 展示进程名称、完整命令、PID、用户、状态、CPU、内存、RSS 和运行时间。
- [x] 进程管理使用独立宽抽屉，不占用左侧服务器监控栏的常驻空间。
- [x] 支持按名称、命令、PID、父 PID、用户和状态搜索。
- [x] 支持按 PID、用户、CPU、内存、RSS 和运行时间排序，默认优先展示高 CPU 进程。
- [x] 支持手动刷新和五秒自动刷新；抽屉关闭后停止自动采集。
- [x] 支持发送 TERM 和 KILL，操作需要二次确认，Rust 端拒绝 PID 0 和 PID 1。
- [x] 进程采集或操作失败只在进程抽屉展示，不关闭终端、SFTP 或基础监控会话。

### P4：高级连接能力

- [x] P4-A：SOCKS5 和 HTTP CONNECT 代理。
- [x] P4-B：跳板机连接。
- [x] P4-C：本地、远程和动态 SOCKS5 端口转发。
- [x] P4-D：SSH Agent 和集中密钥管理。

#### P4-A 验收记录

- [x] 设置窗口提供独立代理管理，可新增、编辑和删除代理资料。
- [x] 代理密码保存在独立的系统凭据服务中，不进入 IndexedDB、配置导出或日志。
- [x] 主机编辑和快速连接可以选择代理，连接历史保留代理引用。
- [x] 删除代理会提示受影响主机，并将对应主机和历史记录恢复为直连。
- [x] SSH 和 SFTP 共用代理传输层，监控继续复用已认证的 SSH 会话。
- [x] SOCKS5 支持远程 DNS 及用户名密码认证；HTTP CONNECT 支持 Basic 认证。
- [x] 连接超时覆盖代理 TCP 建连和协议握手，并区分不可达、超时、认证失败和拒绝连接错误。
- [x] 使用真实测试服务器验证直连、SOCKS5 和 HTTP CONNECT 均可完成 SSH 握手。

#### P4-B 验收记录

- [x] 主机配置提供“连接链路”纵向标签，可在代理和已保存跳板机之间互斥选择。
- [x] 主机、连接历史、备份和配置导入导出均保留跳板机引用，删除跳板机会清理失效引用。
- [x] 保存配置时拒绝自引用、缺失引用、循环引用和多级跳板链路。
- [x] Rust 端通过已认证跳板机建立 `direct-tcpip` 隧道，并使用非阻塞本地回环转发承载目标 SSH 会话。
- [x] 目标终端、SFTP、服务器监控和自动重连共用跳板连接配置。
- [x] 跳板机必须先完成主机指纹信任，认证凭据继续从系统凭据库按跳板机主机 ID 读取。
- [x] 使用真实测试服务器完成第一跳认证、第二跳 SSH 握手、命令执行和 SFTP 主目录读取。

#### P4-C-A 验收记录

- [x] 主机编辑提供独立“端口转发”纵向标签，可维护多条本地监听规则及默认启用状态。
- [x] 主机、连接历史、备份和配置导入导出均保留本地端口转发规则。
- [x] SSH 认证成功后自动启动已启用规则；单条规则校验、重复或端口占用失败不会中断终端连接。
- [x] 本地监听通过当前已认证 SSH 会话的 `direct-tcpip` 通道访问目标，不重复登录，并兼容现有代理或跳板链路。
- [x] 服务器监控工具栏提供端口转发抽屉，可查看运行、停止和失败状态，并逐条启动或停止。
- [x] 关闭标签或 SSH 断开时自动释放监听端口和转发连接，不保留后台监听器。
- [x] 使用真实测试服务器完成本地监听、SSH 通道转发和目标 SSH 服务标识读取。

#### P4-C-B 验收记录

- [x] 主机端口转发页增加“远程转发”标签，可维护服务器监听地址、端口及本地目标。
- [x] 主机、连接历史、备份和配置导入导出均保留远程端口转发规则。
- [x] SSH 认证成功后通过 `forward-listen` 自动创建服务器监听，单条规则失败不会中断终端连接。
- [x] 服务器收到连接后由独立任务连接 FineShell 所在设备可访问的本地目标，避免目标连接超时阻塞终端主循环。
- [x] 每条远程规则限制最多 32 个并发连接，运行时停止会取消服务器监听并关闭已有中继。
- [x] 端口转发抽屉可切换本地与远程规则，并分别查看状态、启动和停止。
- [x] 使用真实测试服务器完成远程回环监听、SSH 转发通道、本地临时 TCP 服务和响应回传。

#### P4-C-C 验收记录

- [x] 主机端口转发页增加“动态转发”标签，可维护 SOCKS5 本地监听地址、端口及默认启用状态。
- [x] 主机、连接历史、备份和配置导入导出均保留动态端口转发规则。
- [x] 使用 `fast-socks5` 处理无认证 SOCKS5 握手和 `CONNECT` 请求，明确拒绝 `BIND` 与 UDP 转发。
- [x] 域名目标原样交给 SSH 服务器解析，客户端地址作为 `direct-tcpip` 通道来源信息传递。
- [x] SOCKS5 握手在异步任务中执行，不阻塞终端主循环；握手和 SSH 通道等待均限制为 10 秒。
- [x] 每条动态规则限制最多 32 个并发连接，运行时停止会释放监听器并关闭已有中继。
- [x] 端口转发抽屉可切换本地、远程与动态规则，并分别查看状态、启动和停止。
- [x] 使用真实测试服务器完成 SOCKS5 协商、SSH 动态转发通道和目标 SSH 服务标识读取。

#### P4-D 验收记录

- [x] 主机编辑和快速连接提供 SSH Agent 认证，连接时使用本机 Agent 已加载的密钥，不保存密码或私钥路径。
- [x] SSH、SFTP 和一级跳板机共用 SSH Agent 认证实现，Agent 不可用或未加载匹配密钥时返回明确错误。
- [x] 设置窗口提供集中密钥管理，可新增、编辑和删除私钥资料，并展示主机与回收站引用数量。
- [x] 集中密钥口令保存在操作系统凭据库，主机只保存密钥 ID，连接时再解析密钥路径。
- [x] 主机、连接历史、备份和配置导入导出均保留集中密钥引用与元数据，导出文件不包含私钥口令。
- [x] 删除仍被主机或回收站引用的密钥会被阻止；旧版直接私钥路径继续兼容并可在编辑时迁移到集中密钥。
- [x] 前端单元测试、生产构建和 Rust 测试通过。

### P5：SFTP 传输与文件编辑

- [x] P5-A：可暂停、继续和取消的传输任务队列。
- [x] P5-B：批量上传下载和拖拽上传。
- [x] P5-C：远程文本文件打开、编辑和安全回传。
- [x] P5-D：使用本地编辑器打开远程文本并在保存后自动同步。
- [x] P5-E：快速目录收藏、路径历史和输入匹配。
- [x] P5-F：远程文件复制、剪切、粘贴和拖拽移动。

#### P5-A 验收记录

- [x] 目录读取和文件管理继续使用主 SFTP 会话，传输使用独立受控通道，大文件传输不会阻塞目录操作。
- [x] 每个会话最多并行两个任务，其余任务进入等待队列。
- [x] 运行任务支持暂停、继续和取消，失败任务继续支持原任务重试。
- [x] 下载先写入本地 `.part` 文件，上传先写入远程 `.part` 文件；取消或失败会清理临时文件。
- [x] 关闭 SFTP 会话会取消该会话所有活动传输任务并唤醒暂停线程。

#### P5-B 验收记录

- [x] 上传文件选择框支持多选，同名远程文件使用一次确认统一覆盖。
- [x] 文件表格勾选多个文件后可批量下载到同一目录，同名本地文件使用一次确认统一覆盖。
- [x] 支持将本地普通文件拖入当前文件列表上传，目录或无效路径会被跳过。
- [x] 传输抽屉紧凑展示每个任务的进度、速度、状态和操作。
- [x] 本地路径检查、前端构建、前端测试和 Rust 测试通过。

#### P5-C 验收记录

- [x] 单个普通文件支持从右键菜单或双击打开远程文本编辑器。
- [x] 仅允许直接编辑不超过 2 MiB 的 UTF-8 文本，并拒绝目录、二进制内容和无效编码。
- [x] 保存前精确比对首次读取的远端内容，外部修改时阻止静默覆盖并要求用户确认。
- [x] 新内容先写入同目录隐藏临时文件并保留原权限，替换前保留同目录备份，重新读取校验成功后再清理备份；失败时自动恢复原文件。
- [x] 编辑器支持未保存关闭保护、字节上限提示和 `Command/Ctrl + S` 保存。
- [x] 前端生产构建、Rust 离线测试和严格 Clippy 检查通过；读取、保存与冲突拒绝流程已通过真实 SFTP 测试服务器验收。

#### P5-D 验收记录

- [x] 文件右键“打开”提供内置编辑器、系统默认应用、设置中的编辑器和临时选择其他应用。
- [x] 设置窗口可保存或清除默认外部编辑器，配置参与版本化存储、导入导出和恢复默认值。
- [x] 远程文件先写入仅当前用户可访问的应用缓存，监听父目录以兼容编辑器使用临时文件替换保存。
- [x] 本地保存经过防抖后复用远程文本安全写回流程，并在文件列表显示监听、同步、成功、冲突或失败状态。
- [x] 传输记录抽屉按文件集中展示外部编辑同步状态、远端路径、更新时间和处理操作，不为每次自动保存重复新增记录。
- [x] 远端内容冲突时停止自动覆盖，可选择保留本地、重新加载远端或用本地内容覆盖远端。
- [x] 断线、同步失败和应用退出不会立即删除本地编辑副本，避免丢失尚未同步的内容。
- [x] 外部编辑不提供手动停止操作；关闭终端标签或退出应用时自动回收监听，断线与重连期间继续保留本地缓存。

#### P5-E 验收记录

- [x] 成功访问远程目录后按主机记录最近路径，失败跳转不会污染历史。
- [x] 路径输入框匹配收藏与历史，支持键盘选择并跳转。
- [x] 文件管理工具栏支持收藏当前目录，并从分组菜单访问快速目录和最近记录。
- [x] 支持取消单个目录收藏和清空当前主机的最近访问记录。
- [x] 收藏和历史存入版本化配置数据库，并参与配置导入导出、备份恢复和主机永久清理。
- [x] 收藏最多保留 20 条，最近访问最多保留 30 条，并自动规范化和去重。

#### P5-F 验收记录

- [x] 文件和目录支持单选或多选复制、剪切，并可跨目录粘贴。
- [x] 同名项目支持自动重命名、覆盖或跳过，默认使用自动重命名。
- [x] 远程目录支持递归复制并保留权限与时间属性，移动优先使用原子重命名，跨文件系统时自动回退为复制后删除。
- [x] 支持将选中的远程项目拖入目录移动，并阻止把目录放入其自身内部。
- [x] 已通过测试服务器在线验证普通文件复制、目录递归复制和同名覆盖保护。

### P6：终端效率工具

- [x] P6-A：终端内容搜索与高亮。
- [x] P6-B：快捷命令面板和带动态参数的自定义命令。
- [x] P6-C：终端配色方案、行高和交互偏好。

#### P6-A 验收记录

- [x] `Command/Ctrl + F` 打开当前会话的终端搜索，`Escape` 关闭并恢复终端输入焦点。
- [x] 使用 xterm 搜索插件匹配当前回滚缓冲区，区分当前匹配与其他匹配并显示结果计数。
- [x] 支持按钮、`Enter` 和 `Shift + Enter` 在匹配项间向后或向前跳转。
- [x] 支持区分大小写，并在终端持续输出时更新匹配结果。
- [x] 搜索查询和大小写选项按会话独立保存，多会话之间互不干扰。
- [x] 前端测试、生产构建和 macOS 调试应用实机交互验收通过。

#### P6-B 验收记录

- [x] 设置窗口提供快捷命令管理，可维护名称、分组、命令模板和备注。
- [x] 命令模板支持 `{{参数}}` 和 `{{参数:默认值}}`，重复参数只填写一次。
- [x] 终端标签栏右侧提供固定快捷命令入口，支持名称、分组、备注和模板搜索。
- [x] 快捷命令可填入当前终端继续编辑，也可追加回车直接执行；动态参数在发送前集中填写并预览最终命令。
- [x] `Command/Ctrl + Shift + P` 可打开当前会话的快捷命令抽屉，关闭后恢复终端输入焦点。
- [x] 快捷命令纳入版本化配置数据库、导入导出和自动备份恢复。
- [x] 模板解析与配置迁移单元测试、TypeScript 检查、生产构建和 macOS 调试应用实机交互验收通过。

#### P6-C 验收记录

- [x] 提供 FineShell 深色、石墨浅色、Solarized 深色和 Dracula 四套完整终端配色，并同步更新终端背景、光标、选择区和 ANSI 色板。
- [x] 支持在 1.0 至 2.0 之间调整终端行高，字体、字号、行高、光标、回滚行数和配色可即时作用于已打开会话。
- [x] 支持可选的选中后复制，以及“显示操作菜单”或“直接粘贴”两种终端右键行为。
- [x] 终端右键菜单提供复制、粘贴和全选，并根据选区和连接状态禁用不可用操作。
- [x] 桌面端剪贴板使用 Tauri 官方插件，并仅开放文本读取和写入权限。
- [x] 新设置纳入版本化配置、导入导出和自动备份恢复；旧备份恢复时保留当前终端偏好。
- [x] 前端单元测试、TypeScript 检查、生产构建、Rust 测试、严格 Clippy 和 macOS 调试应用启动验收通过。

### P7：安全与诊断

- [x] P7-A：已知主机指纹集中管理。
- [x] P7-B：系统凭据清理与连接历史保留策略。
- [x] P7-C：日志分级、敏感信息脱敏和可导出的诊断日志。

#### P7-A 验收记录

- [x] 设置窗口新增“已知主机”，集中展示目标地址、关联主机、SHA256 指纹、首次记录和最近验证时间。
- [x] 支持按地址、端口、主机名称或指纹搜索，并可复制或单独移除指纹。
- [x] 支持清理未关联已保存主机的记录，操作前明确提示快速连接记录也会受影响。
- [x] 指纹按地址和端口统一管理，同一服务器的不同账号共享信任状态。
- [x] 移除指纹会原子清除已保存主机、连接历史和已打开会话中的旧信任；下次连接或自动重连重新要求确认。
- [x] 首次连接与指纹变更使用不同警告级别，变更对话框明确展示服务器、原指纹和新指纹，且不会静默替换。
- [x] 旧配置、旧导出文件和旧备份可自动迁移，已知主机记录参与新版导入导出和自动备份恢复。
- [x] 57 项前端测试、TypeScript 检查、生产构建、44 项离线 Rust 测试、严格 Clippy、Tauri 调试整包构建和桌面端入口验收通过。

#### P7-B 验收记录

- [x] 设置窗口新增“隐私与清理”，连接历史可以选择 20、50、100 条或不限数量，以及 7、30、90 天或永久保留。
- [x] 新策略会在配置载入、设置保存、历史新增和配置导入导出时统一执行，旧配置继续保持最近 50 条且永久保留的原有行为。
- [x] 支持明确确认后清空全部连接历史，不影响已保存主机；历史变化会同步刷新主窗口。
- [x] 系统凭据使用不含敏感值的本机索引登记，扫描时只向前端返回凭据是否存在，不返回密码或私钥口令。
- [x] 可以识别失去主机、快速连接、集中密钥或代理关联的遗留凭据，并支持逐项或批量确认清理。
- [x] 凭据索引不进入配置导出文件，旧配置和导入配置可通过主动扫描重新计算本机凭据关联。
- [x] 61 项前端测试、TypeScript 检查、生产构建和 45 项离线 Rust 测试通过；8 项依赖在线环境变量的 Rust 测试保持忽略。

#### P7-C 验收记录

- [x] Rust 后端提供最多 1000 条记录的内存环形缓冲区，应用退出后自动清除，连续重复错误会在 10 秒内合并计数。
- [x] 支持调试、信息、警告和错误四个级别；高频终端输入、尺寸变化和监控采样不会产生调试生命周期日志。
- [x] 前端关键 Tauri 命令统一记录失败上下文，并补充 SSH 会话、SFTP 传输、外部编辑同步和全局运行时异常记录。
- [x] 前后端分别执行脱敏，覆盖 URL 凭据、密码和口令字段、私钥块、主机地址、用户名、本地路径及命令参数。
- [x] 设置窗口新增“高级”，可以查看日志数量、错误和警告摘要，调整日志级别，并清空或导出本次运行的诊断日志。
- [x] 导出日志包含应用版本、平台、架构和脱敏记录，不包含配置、密码、私钥或系统凭据内容。
- [x] 64 项前端测试、TypeScript 检查、生产构建、50 项离线 Rust 测试、严格 Clippy 和 macOS 调试应用打包通过；8 项依赖在线环境变量的 Rust 测试保持忽略。

### P8：工程质量与跨端协议

- [x] P8-A：前后端共享命令、事件、协议版本和结构化错误边界。
- [ ] P8-B：关键 React 组件测试和 Tauri 命令集成测试。
- [ ] P8-C：桌面端端到端测试和三平台兼容验证。

#### P8-A 验收记录

- [x] 根目录提供机器可读的版本化协议清单，集中登记 54 个 Tauri 命令、7 个事件和 13 个错误码。
- [x] 前端命令调用只接受契约中的命令名，事件监听与发送按事件名自动推导载荷类型，不再由业务组件直接调用 Tauri 事件 API。
- [x] 前端统一使用 `FineShellCommandError`，保留错误码、操作名、是否可重试和可选上下文；旧 SSH/SFTP 字符串错误在协议边界兼容归类。
- [x] Rust 提供同构错误码与协议版本命令，配置文件和系统凭据命令已返回结构化错误；SSH/SFTP 内部实现可继续使用现有错误字符串。
- [x] SSH、SFTP 与端口转发事件名迁移为 Rust 协议常量，避免各业务模块重复声明。
- [x] 应用初始化校验前后端协议版本，版本不一致时停止继续读取工作区并给出明确错误。
- [x] Rust 测试从真实 Tauri 注册表提取命令，与共享契约逐项核对；前端覆盖结构化错误和旧错误兼容归类。
- [x] 67 项前端测试、TypeScript 检查、生产构建和 52 项离线 Rust 测试通过；8 项依赖在线环境变量的 Rust 测试保持忽略。

## 五、下一步建议

P0、P1、P2-A 至 P2-C、P3-A 至 P3-C、P4-A 至 P4-D、P5-A 至 P5-F、P6-A 至 P6-C、P7-A 至 P7-C、P8-A 已完成。下一阶段建议实现 P8-B 自动化测试基础：优先覆盖主机管理、会话标签、文件管理和传输抽屉的关键 React 交互，并为配置、凭据、SSH/SFTP 会话生命周期建立可隔离的 Tauri 命令集成测试。

完成 P1 后，按以下顺序扩展：

1. 主机配置数据库、备份和回收站。
2. 分组树、排序和导入导出。
3. 基础服务器监控。
4. 跳板机和端口转发。

## 六、设置窗口后续规划

按现有功能边界，后续适合逐步加入以下设置：

1. 外观：跟随系统的亮色/暗色主题、界面密度和强调色。
2. 终端：配色方案、行高、复制选择区、右键粘贴、提示音和字符编码。
3. 文件传输：默认下载目录、同名文件策略、并发任务数、完成通知；需要先完成可取消的传输任务队列。
4. 连接：代理、跳板机、本地/远程/动态端口转发、SSH Agent 和集中密钥已完成；后续增加转发默认值。
5. 监控：趋势保留时长、采样历史上限和资源阈值通知。
6. 安全：已知主机指纹、凭据清理、历史记录保留周期和诊断信息脱敏已完成。
7. 高级：日志级别和诊断日志导出已完成；后续增加更新通道和自动更新。
