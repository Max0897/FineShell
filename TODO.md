# FineShell 开发 Todo List

> 基线日期：2026-07-22
>
> 对照文档：`FEATURES.md`
>
> 当前阶段：桌面应用原型，已完成核心界面与主机管理流程，尚未接入真实 SSH、终端和 SFTP 能力。

## 状态说明

- `[x]` 已实现：已有可操作代码，并完成基础编译检查。
- `[~]` 部分实现：界面或本地流程已完成，但缺少真实后端能力或完整交互。
- `[ ]` 未实现：当前代码中没有对应功能。

## 一、当前已经实现

### 1. 应用基础

- [x] 使用 Tauri 2、React、TypeScript 和 Vite 初始化桌面项目。
- [x] 接入 Arco Design React 组件库。
- [x] 使用 Arco `ResizeBox.SplitGroup` 构建三块可调整布局：左侧服务器信息、右上终端、右下 SFTP。
- [x] 主页面没有应用内 Header。
- [x] 应用内组件不使用阴影，保留操作系统原生窗口阴影。
- [x] 已处理 SFTP 路径输入框聚焦时的粗边框问题。
- [x] 前端生产构建和 Tauri Rust 编译检查可以通过。

### 2. 主机管理

- [x] 主机管理使用独立 Tauri 原生窗口，不使用 UI Drawer。
- [x] 应用启动时自动打开主机管理窗口。
- [x] 主机管理与主窗口可以独立聚焦和自由点击。
- [x] 重复打开主机管理时复用已有窗口并切换到指定标签。
- [x] 支持新增、编辑和删除主机。
- [x] 支持按名称、地址、用户名或分组搜索主机。
- [x] 支持主机名称、地址、端口、用户名和分组字段。
- [x] 主机信息使用 `localStorage` 持久化。
- [x] 支持快速连接表单。
- [x] 支持最近连接历史和历史重连入口。
- [x] 连接历史去重并限制为最近 50 条。

### 3. 主窗口会话界面

- [x] 主机管理与主窗口通过 Tauri 事件传递连接目标。
- [x] 连接目标会在主窗口创建终端会话标签。
- [x] 相同用户名、地址和端口不会重复创建会话标签。
- [x] 终端标签支持切换、关闭和新增入口。
- [x] 终端区域始终显示多标签栏，没有“终端”标题和“打开连接”按钮。
- [x] 左侧服务器信息面板显示当前主机、地址、协议、用户名、端口、分组和打开时间。
- [x] 支持从服务器信息面板关闭当前会话。

## 二、部分实现

### 1. 远程连接管理

- [~] 已有主机配置和连接入口，但当前“连接”只创建前端会话，没有建立真实 SSH 连接。
- [~] 已有多标签和多服务器界面，但没有连接状态、超时、失败原因、断线或重连状态机。
- [~] 已有基础分组字段，但没有分组树、拖拽排序、复制、剪切和粘贴。
- [~] 已有快速连接，但只包含地址、用户名和端口，不支持密码或密钥认证。

### 2. 终端

- [~] 已有多标签终端容器和静态命令提示符。
- [~] 已有会话打开和关闭流程，但没有远程 PTY、输入输出流和终端尺寸同步。
- [~] 当前终端内容是展示占位，不是真实可输入终端。

### 3. SFTP

- [~] 已完成终端与 SFTP 同屏布局。
- [~] 已有路径栏、返回上级、刷新、新建目录、上传按钮和文件表格结构。
- [~] 按钮会根据是否存在会话显示启用或禁用状态，但没有连接到真实 SFTP 服务。

### 4. 服务器信息

- [~] 左侧面板可以展示保存的连接元数据。
- [~] 尚未从服务器采集操作系统、硬件、运行时间或资源指标。

### 5. 本地配置

- [~] 主机和连接历史可以保存在浏览器 `localStorage`。
- [~] 当前没有凭据加密、配置迁移、自动备份、回收站或损坏恢复机制。

## 三、尚未实现

### 1. SSH 与连接能力

- [ ] 密码认证。
- [ ] 私钥认证、私钥口令和密钥管理。
- [ ] 主机指纹校验、首次连接确认和指纹变更警告。
- [ ] 代理服务器和 SOCKS/HTTP 代理。
- [ ] 跳板机连接。
- [ ] 本地、远程和动态端口转发。
- [ ] Windows 远程桌面连接。
- [ ] SSH 保活、连接超时、断线检测和自动重连。

### 2. 完整终端能力

- [ ] 真实远程 Shell 和 PTY。
- [ ] 键盘输入、复制粘贴、窗口尺寸变化同步。
- [ ] ANSI 颜色、光标、选择区和全屏程序支持。
- [ ] 命令历史、自动补全和智能匹配。
- [ ] 快捷命令面板和带动态参数的自定义命令。
- [ ] 终端内容搜索与高亮。
- [ ] 字体、配色方案和背景设置。
- [ ] Zmodem `rz`、`sz` 文件传输。

### 3. 完整 SFTP 能力

- [ ] 远程目录读取和路径导航。
- [ ] 上传、下载、进度显示、暂停、取消和失败重试。
- [ ] 拖拽上传和拖拽移动。
- [ ] 新建、重命名、移动和删除文件或目录。
- [ ] 文件权限查看与修改。
- [ ] 远程文本文件打开、编辑和回传。
- [ ] 文件夹打包传输、自动压缩和解压。
- [ ] 跟随终端当前目录。
- [ ] 快速目录和路径历史匹配。

### 4. 服务器监控

- [ ] CPU、内存、磁盘和网络流量采集与展示。
- [ ] Ping 延迟、丢包和 Traceroute。
- [ ] 进程列表、搜索、排序和进程管理。
- [ ] 端口监听和网络连接状态。
- [ ] 操作系统、硬件和运行时间信息。
- [ ] 不同 Linux/Unix 系统命令输出的兼容解析。

### 5. 网络加速

- [ ] SSH 和远程桌面连接加速。
- [ ] 加速节点选择和自动切换。
- [ ] 提供给第三方 SSH 客户端使用的本地转发入口。

### 6. 云端同步与配置保护

- [ ] 用户账号和同步服务。
- [ ] 主机、代理、密钥、快捷命令和历史记录同步。
- [ ] 本地加密、同步密码、完整性校验和冲突解决。
- [ ] 本地配置自动备份和恢复。
- [ ] 删除连接回收站及 30 天恢复机制。

### 7. 工程质量

- [ ] 前后端共享的连接命令、事件和错误类型定义。
- [ ] 敏感数据安全存储，禁止密码和私钥进入 `localStorage`。
- [ ] 日志分级、敏感信息脱敏和可导出的诊断日志。
- [ ] 单元测试、组件测试、Rust 集成测试和桌面端端到端测试。
- [ ] Windows、macOS 和 Linux 的兼容性测试。
- [ ] 安装包、应用图标、签名、自动更新和发布流程。
- [ ] 拆分前端大体积资源，处理当前构建包体积警告。

## 四、建议实施顺序

### P0：完成一条真实 SSH 连接闭环

这是下一步最高优先级。当前不建议继续增加界面功能，应该先让一个保存的主机能够真正连接并操作远程 Shell。

- [ ] 扩展主机模型，加入认证方式、连接超时和主机指纹信息。
- [ ] 在 Rust 端建立 SSH 连接服务和会话生命周期管理。
- [ ] 定义 `connecting`、`connected`、`failed`、`disconnected`、`reconnecting` 状态。
- [ ] 建立前端与 Rust 之间的输入、输出、尺寸变化和关闭事件协议。
- [ ] 接入成熟的终端渲染组件，承载 ANSI、光标和键盘交互。
- [ ] 将当前静态终端面板替换为真实终端实例。
- [ ] 在标签和服务器信息面板中展示连接状态及错误。
- [ ] 实现主动断开、异常断开和资源释放。

#### P0 验收标准

- 用户可以保存一个密码或密钥认证的 SSH 主机。
- 点击连接后能看到明确的连接中、成功或失败状态。
- 连接成功后可以执行命令并持续接收输出。
- 调整窗口大小后远程 PTY 尺寸正确同步。
- 关闭标签后连接、任务和事件监听均被释放。
- 密码、私钥和口令不以明文写入 `localStorage` 或日志。

### P1：实现基础 SFTP

- [ ] 复用主机认证配置建立 SFTP 会话。
- [ ] 实现目录列表、路径导航和刷新。
- [ ] 实现单文件上传、下载及进度显示。
- [ ] 实现新建目录、重命名和删除。
- [ ] 处理覆盖确认、权限不足、断线和重试。

### P2：补齐主机管理与可靠性

- [ ] 增加密码、密钥、代理和跳板机配置界面。
- [ ] 增加分组树、排序、复制和导入导出。
- [ ] 将普通配置迁移到版本化本地数据库。
- [ ] 将敏感凭据迁移到操作系统安全存储。
- [ ] 增加配置备份、恢复和删除回收站。
- [ ] 增加连接保活、重连和主机指纹安全流程。

### P3：监控和高级功能

- [ ] 先实现系统信息、CPU、内存和磁盘的基础采集。
- [ ] 再实现网络、进程、端口和路由追踪。
- [ ] 在核心 SSH/SFTP 稳定后，再评估远程桌面、网络加速和云同步。

## 五、下一步建议

下一项开发任务建议定为：**实现“单主机、单标签、密码认证”的真实 SSH 终端最小闭环**。

建议把范围限制为 Linux/macOS SSH 服务器、密码认证、一个终端标签和手动断开。暂时不加入密钥、代理、跳板机、监控和自动重连。完成这条闭环后，项目会从界面原型升级为可实际使用的 SSH 客户端，也能尽早验证 Rust SSH 库、PTY、事件传输、终端渲染和资源释放这些最关键的技术风险。

完成最小闭环后，按以下顺序扩展：

1. 密钥认证和主机指纹校验。
2. 多标签并发连接和重连。
3. 基础 SFTP 目录与传输。
4. 主机配置安全存储和备份。
5. 基础服务器监控。
