# 更新日志

本文件记录 FineShell 各版本的重要变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

### 修复

- 修复 AI 混合调用诊断工具与受控操作时可能出现编排中断、无效工具调用或前后端任务状态不同步的问题。
- 修复 Windows 环境下新建、复制及传输远程文件时可能错误使用反斜杠的问题，统一按 SFTP POSIX 路径处理。
- 修复 macOS 依次关闭主窗口和辅助窗口后，点击 Dock 图标无法重新显示主窗口的问题。
- 修复首页主机列表未占满可用区域，导致表头与表格滚动范围异常的问题。

### 优化

- 收敛 AI Agent 工具契约、动作轮次结算和事件同步流程，将可恢复的工具错误返回给模型继续处理，并增强任务恢复稳定性。
- 统一 macOS、Windows 和 Linux 的应用图标资源。

### 工程

- 补充 AI Agent 混合工具调用、异常恢复、工具契约与事件同步回归测试。

## [0.1.18] - 2026-08-06

### 新增

- AI Agent 新增结构化 systemd 服务状态与服务日志诊断工具，优先通过受控只读通道采集服务信息。
- AI 对话接入模型返回的实际 Token 用量，提升上下文用量展示与会话记录的准确性。

### 修复

- 修复部分合法终端命令因工具参数结构差异被误判为“AI 终端命令无效”的问题。
- 修复 CPU 与内存资源趋势因堆叠显示导致波形失真，网络上下行趋势继续使用累计堆叠展示。
- 修复远程工具输出可能被模型误识别为可信指令的问题，强化不可信结果的边界标记与注入防护。
- 修复版本准备工作流因权限检测、依赖安装或自动创建 PR 后未触发保护检查而失败的问题。
- 修复普通配置或脚本变更可能误触发已有版本重复发布的问题。

### 优化

- 增强 AI 任务上下文时效校验，在会话或服务器上下文变化后刷新执行快照，避免使用过期状态继续操作。
- 拆分 AI Provider 适配层，统一不同模型提供商的消息格式、工具调用与推理上下文续传逻辑。

### 工程

- 补齐 AI Agent 从诊断、工具执行、审批到结果恢复的全流程场景测试。
- 新增自动化发布准备流程，统一升级应用版本、归档更新日志并创建受保护的发布 Pull Request。

## [0.1.17] - 2026-08-06

### 新增

- 新增应用暗色主题，支持浅色、深色及跟随系统，并统一终端、文件管理、设置窗口和 AI 面板的主题表现。
- 新增终端会话日志，可将终端输出按会话持续保存到指定目录，并提供默认日志位置和日志管理设置。
- 主窗口更新流程新增安装包下载进度，展示已下载大小、总大小及百分比。

### 修复

- 修复暗色模式打开 AI 面板时短暂白屏或与终端内容重叠的问题。
- 修复终端全屏或隐藏服务器监控栏、文件管理栏后出现边缘白线的问题。
- 修复使用 Shell 原生 `Tab` 补全或方向键召回历史后，Ghost Text 无法继续自动补全的问题。
- 修复部分推理模型在连续工具调用时缺少推理上下文，以及模型返回未启用工具调用导致任务中断的问题。

### 优化

- 重构 AI Agent 执行、审批与恢复流程，取消固定回合限制，并使用独立 SSH 通道执行受控命令，避免干扰用户终端。
- 精简 AI 审批卡片、命令输出和生成状态，补充可折叠思考过程、任务恢复与执行结果反馈。
- 优化应用标题栏、AI 面板入口和工具菜单布局，减少常驻按钮并统一跨平台窗口交互。
- 更新 macOS 专用应用图标，保持 Windows 与 Linux 图标资源不变。

### 工程

- 扩展 Agent 生命周期、任务恢复、命令审批、独立 SSH 执行、终端日志和 Ghost Text 召回测试。

## [0.1.16] - 2026-08-04

### 新增

- 终端新增基于本地主机命令历史的 Ghost Text 自动补全，支持使用 `Tab` 接受建议、`Esc` 取消建议，并按当前主机与远程目录优先匹配。
- 终端支持使用 `Command/Ctrl + +/-` 或配合鼠标滚轮临时调整字号，使用 `Command/Ctrl + 0` 恢复设置字号。
- SFTP 地址栏改为完整路径面包屑，每级目录均可点击跳转，点击尾部空白区域可切换为完整路径输入。
- 新增跨平台应用标题栏：macOS 保留原生窗口按钮，Windows 与 Linux 使用自定义窗口控制和统一工具入口。

### 修复

- 修复终端背景上、左、下边缘颜色不一致的问题。
- 修复 SSH 短暂探测超时被过早判定为断开，导致终端、监控和 SFTP 面板出现干扰性遮罩的问题。

### 优化

- 断连反馈改为不影响原有布局的状态提示，并提供带加载反馈的重新连接操作。
- 设置窗口使用左侧子菜单承载原有页内标签，优化已知主机指纹列、省略显示和菜单滚动体验。
- 调整服务器监控栏与 SFTP 面板的显隐入口，统一到标题栏工具按钮。
- 拆分 SSH、SFTP、AI Agent、配置变更、终端会话与主窗口等大型模块，并对辅助窗口和主要面板进行按需加载。

### 工程

- 补充终端历史匹配、Ghost Text、字号缩放、SFTP 面包屑、连接健康状态和窗口能力测试。

## [0.1.15] - 2026-08-03

### 修复

- 修复应用完成 OTA 更新并重启后，主窗口可能停留在其他应用下方的问题。
- 修复 Windows 剪贴板历史选择内容后，终端收到 `^V` 而未粘贴文本的问题。

### 优化

- 更新重启后等待工作区就绪再恢复主窗口，并在聚焦失败时请求系统提醒；普通启动不会抢占焦点。
- Windows 终端使用系统剪贴板完成 `Ctrl + V` 粘贴，同时保留 xterm 的换行处理与 Bracketed Paste 支持。

## [0.1.14] - 2026-08-03

### 新增

- 设置窗口新增 GitHub 更新镜像线路，可按需启用内置线路加速版本检查与安装包下载。
- 更新线路支持独立测速，便于在当前网络环境下选择可用线路。

### 修复

- 修复开发环境中更新线路测试按钮不可用的问题。

### 优化

- 主窗口自动检查更新与手动更新统一使用所选镜像线路，并在镜像不可用时回退 GitHub 官方地址。
- 移除不可用的 `githubproxy.cc` 线路。

## [0.1.13] - 2026-08-03

### 新增

- 首页主机列表支持双击直接创建 SSH 会话。

### 优化

- 全局界面默认禁止无意义的文本选中，终端、输入控件及明确需要复制内容的区域继续支持选择。
- 完善 AI 助手的使用说明与运行界面展示。

## [0.1.12] - 2026-07-30

### 新增

- 新增由 Rust 后端统一管理的 AI Agent 任务运行时，覆盖任务状态、工具调用、审批等待、执行结果与断线恢复。
- AI 受控操作支持“请求审批”“替我审批”和“完全访问”三种权限模式；需要人工确认时统一使用同意、驳回和补充说明审批卡片。
- 新增单次审批凭证、可信动作意图、执行验证证据和业务目标验证，避免授权被重复使用或操作结果未经验证便标记完成。

### 修复

- 修复 AI 审批与终端命令生命周期衔接不稳定，导致命令完成后仍等待结果或重复请求工具的问题。
- 修复 AI 任务中断后缺少安全恢复边界的问题；恢复时会校验任务快照、事件序列与待审批状态。

### 优化

- 使用 Rig 整理模型调用与工具编排，将只读诊断、终端命令和远程文件操作逐步收口到后端策略与执行边界。
- 限制 AI 工具循环、连续失败和自动修复次数，并在达到预算后使用已收集证据生成结论。
- 简化 AI 面板，移除操作审计抽屉和服务器监控快照 AI 分析入口；进程与网络诊断交接功能保持不变。
- 支持双击分隔栏隐藏或恢复服务器监控栏和 SFTP 面板，并恢复用户最近一次拖拽尺寸。
- 补充 Linux `.deb` 使用 APT 安装依赖及修复未配置软件包的说明。

### 工程

- 扩展前后端共享协议，补充 Agent 生命周期、审批策略、可信执行、断线恢复和分栏交互测试。

## [0.1.11] - 2026-07-29

### 新增

- 新增 AI 运维助手，支持 OpenAI 兼容接口、服务预设、模型发现、流式对话、对话历史与长对话自动摘要。
- AI 助手支持按需提及终端选区、最近终端输出、服务器状态、当前远程目录和远程文件，并在发送前隐藏疑似密钥、令牌与命令中的敏感内容。
- 新增服务器状态、进程、SFTP 目录和网络连接等 AI 只读诊断工具；Ping、路由追踪及多步骤诊断计划会在执行前展示并等待确认。
- 新增 AI 终端命令提案与结果分析，命令经过本地风险分级后仅填入终端，不会自动执行。
- 新增 AI 远程文件修改、新建、重命名和删除提案，支持差异审阅、逐项或批量应用、安全回滚及操作审计。
- 默认启用 Shell Integration，支持采集命令退出状态，并将终端当前目录自动同步到 SFTP 文件管理器。
- SFTP 支持从 Finder 或文件资源管理器拖入本地文件和文件夹，递归上传时保留目录结构与空目录。

### 修复

- 修复 Shell Integration 安装脚本可能直接显示在终端中的问题。
- 修复 AI 侧栏调整宽度时影响左侧服务器监控栏最小宽度，以及上下文提及与正文间距异常的问题。
- 修复 AI 工具执行耗时显示为异常超大数值的问题。
- 修复 macOS Retina 环境下拖拽文件到 SFTP 面板时落点判断不准确的问题。

### 优化

- 多文件上传改为批次处理，批次结束后仅提示和刷新一次，避免上传大量小文件时反复弹出消息和刷新列表。
- 完善 AI 服务能力探测、Token 预算展示、权限控制、上下文交接、文件审阅和诊断计划体验。
- 优化诊断日志管理，改为直接使用系统默认编辑器打开日志文件。
- 新增主机时可直接选择已有的嵌套分组路径。

### 工程

- 补充 AI 编排、受控操作、Shell Integration、SFTP 拖拽上传和上传批次的前后端测试。
- 修复新版 Clippy 严格检查告警。

## [0.1.10] - 2026-07-27

### 新增

- 密钥管理支持直接粘贴 OpenSSH、RSA、EC、DSA 和 PKCS#8 私钥，由 FineShell 托管后用于终端、SFTP 与跳板机认证。
- 新增托管私钥格式校验、大小限制及安全落盘；Unix 平台使用 `0700` 目录权限和 `0600` 文件权限。

### 修复

- 修复 Windows 主窗口缺少更新权限，导致启动时无法自动检查新版本的问题。

### 优化

- 删除托管密钥记录时同步清理私钥文件与系统凭据，并保持已有文件路径密钥和旧配置兼容。

## [0.1.9] - 2026-07-27

### 优化

- 移除主窗口标签栏中的诊断日志导出入口，减少常驻操作按钮；诊断日志仍可在设置窗口中导出。

### 工程

- 发布工作流统一预创建草稿 Release，避免并行构建产生同版本的多个草稿和不完整更新清单。

## [0.1.8] - 2026-07-27

### 修复

- 修复 Windows x64 与 ARM64 环境中设置、快捷键窗口仍可能白屏且标题栏关闭按钮无响应的问题。
- Windows 辅助窗口改为随应用启动预加载并隐藏，打开时直接显示已有窗口，避开 WebView2 运行时创建窗口的阻塞问题。
- 辅助窗口关闭时改为隐藏并复用，关闭 Windows 与 Linux 主窗口时仍会正常退出应用。

### 优化

- 主窗口新增诊断日志导出入口，辅助窗口异常时也可以直接导出脱敏日志。
- 启动日志增加窗口列表，辅助窗口显示与创建过程增加原生日志，便于定位跨平台窗口问题。

## [0.1.7] - 2026-07-27

### 修复

- 修复 Windows x64 与 ARM64 环境中设置、快捷键窗口可能白屏且关闭按钮无响应的问题。
- 辅助窗口改用异步命令创建，并通过 URL 片段稳定选择对应界面。
- 增加应用级错误边界，界面加载失败时显示可恢复的错误提示。

### 优化

- 补充主机保存、系统凭据写入和本地配置落盘的脱敏诊断日志。

## [0.1.6] - 2026-07-26

### 工程

- 增加 Linux ARM64 `.deb` 与 Windows ARM64 `.exe` 安装包构建。
- Linux 发布包统一为 `.deb`，不再构建 AppImage。

## [0.1.5] - 2026-07-26

### 修复

- 修复更新清单使用 GitHub API 资产地址，导致安装包下载可能因匿名请求限流返回 403 的问题。

## [0.1.4] - 2026-07-26

### 修复

- 修复 Windows 环境中设置和快捷键操作窗口仍可能白屏、标题栏关闭按钮无响应的问题。

## [0.1.3] - 2026-07-26

### 修复

- 修复 Windows 环境中设置和快捷键操作窗口打开后白屏的问题。
- 修复 macOS 检查更新时因发布清单缺少平台更新包而失败的问题。
- 移除 Windows 与 Linux 环境中的原生系统菜单栏，macOS 继续保留系统菜单。

## [0.1.2] - 2026-07-26

### 修复

- 修复正式安装包中设置窗口标题栏关闭按钮无响应的问题。

## [0.1.1] - 2026-07-26

### 修复

- 修复 macOS 安装包错误依赖构建机 Homebrew OpenSSL，导致安装后无法启动的问题。

### 工程

- macOS 发布构建改为静态链接 vendored OpenSSL。
- 增加 macOS 发布产物动态依赖检查，阻止包含构建机本地依赖的安装包发布。

## [0.1.0] - 2026-07-25

FineShell 的首个公开版本，将 SSH 终端、SFTP 文件管理和服务器监控整合到统一的跨平台桌面工作区。

### 下载

- macOS：Apple Silicon / Intel `.dmg`
- Linux：x64 `.AppImage` / `.deb`
- Windows：x64 `.exe`（NSIS）/ `.msi`

### 新增

- 支持密码、私钥、托管密钥和 SSH Agent 认证。
- 支持 SOCKS5、HTTP CONNECT 代理及一级跳板机连接。
- 支持多主机、多会话终端标签，以及本地、远程和动态 SOCKS5 端口转发。
- 支持 SFTP 上传下载、批量拖拽、暂停继续、取消重试、复制移动和权限管理。
- 支持远程文本文件内置编辑和本地编辑器自动同步。
- 支持远程压缩、解压和目录打包下载。
- 支持 CPU、内存、磁盘、网络流量监控，以及进程管理和网络诊断。
- 支持主机分组、连接历史、配置导入导出、自动备份、回收站和已知主机指纹管理。
- 支持终端搜索、参数化快捷命令和快捷键操作说明。
- 支持应用内检查 GitHub Release 更新并下载安装。

### 工程

- 增加前端测试、Rust 测试、Clippy 检查和 GitHub Actions 持续集成。
- 增加 macOS、Linux 和 Windows 跨平台安装包构建及签名更新清单。

### 已知限制

- macOS 安装包当前使用 ad-hoc 签名，尚未接入 Apple 公证。
- Windows 安装包尚未配置商业代码签名证书。
